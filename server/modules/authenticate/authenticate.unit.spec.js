let testConfig = require('../../configs/test.config');
let expect = testConfig.expect;
let authFB = require('./auth.fb.js');

describe('Authenticate Unit', function() {

	describe('Logout', function() {
		it('passport logout', done => {
			done();
		})
	});

	describe('Login', function() {
		it('passport login', done => {
			done();
		})
	});

	describe('Sign up', function() {
		it('passport signup', done => {
			done();
		})
	});
});